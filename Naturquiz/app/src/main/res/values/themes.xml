<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Naturquiz" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bla_mork</item>
        <item name="colorPrimaryVariant">@color/bla_lys</item>
        <item name="colorOnPrimary">@color/svart</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/hvit</item>
        <item name="colorSecondaryVariant">@color/rod_lys</item>
        <item name="colorOnSecondary">@color/svart</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/svart</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Starting activity theme -->
    <style name="Theme.Naturquiz.Launcher">
        <item name="android:windowBackground">@drawable/oppstart</item>
    </style>
</resources>